
<h1 style="padding-top: 100px">Welcome to The Gossip Project</h1>


<div>
<% @gossips.each do |gossip| %>
<div>
    <div class="card-header"><%= User.find(gossip.user_id).first_name %>  <%= User.find(gossip.user_id).last_name %>  </div>
    <div class="card-body">
      <h4 class="card-title"> <%= link_to "#{gossip.title}", "gossip/#{gossip.id}" %> </h4>
    </div>
  </div>
  <% end %>
  </div>

  